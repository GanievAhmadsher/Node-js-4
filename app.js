import path from "node:path";
import fs from "node:fs";
const activePath = path.join(process.cwd(), "dars2.wmv");
const activeWrite = path.join(process.cwd(), "fs2.mp4");
// fs.readFile(activePath, (err, data) => {
//   fs.writeFile(activeWrite, data, (err, data) => {
//     console.log(err);
//   });
// });

// fs.readFile(activePath, (err, data) => {
//   fs.writeFile(activeWrite, data, (err) => {
//     console.log(err);
//   });
// });

// let buf = Buffer.from("ganievahmadsher6205@gmail.com");
// console.log(buf);
// console.log(buf.toString("utf-8"));
// async function name(params) {
//     fs.readFile(activePath,'utf-8')
// }

// fs.statSync(); // ? file info
// fs.existsSync(); // ? file (has or don't have)

// fs.readFile()